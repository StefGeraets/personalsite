---
layout: base.njk
permalink: /
eleventyNavigation:
  key: home
  order: 100
---

<div class="hero">
<h1>Home <small>hopefully</small></h1>
<p>Test if this is the homepage because i set the permalink</p>
</div>



{%- for item in collections.experiences -%}
  <h4>{{ item.data.title }}</h4>
  {% if item.data.fromDate %}
    <p>{{ item.data.fromDate }} - {{ item.data.toDate}}</p>
  {% endif %}
  {% if item.data.tech %}
    <em>{{ item.data.tech}}</em>
  {% endif %}
  {{ item.templateContent | safe }}
{%- endfor -%}