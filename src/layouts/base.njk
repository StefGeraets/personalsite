<!DOCTYPE html>
<html class="no-js" lang="{{ site.lang }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ site.description }}">

  <title>{%- if title -%}{{ title }} | {{ site.title }}{%- else -%}{{ site.title }}{%- endif -%}</title>
  <link rel="preload" href="{{ '/assets/fonts/poppins-regular.woff2' | url }}" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="{{ '/assets/fonts/poppins-bold.woff2' | url }}" as="font" type="font/woff2" crossorigin>

  <link rel="stylesheet" href="/css/main.css" />

  <script preload defer src="/js/alpine.js"></script>
</head>
<body>

  <div class="layout" role="document">
    {% include "header.njk" %}

    <main class="main container" role="main">
      {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}
  </div>

  <script type="module" src="/js/main.js"></script>

</body>
</html>